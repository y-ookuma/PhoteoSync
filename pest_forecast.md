# pest_forecast.html 説明書（病害虫発生予察ツール）

PhoteoSyncの病害虫発生予察機能（`pest_forecast.html`）が「何のデータを使い、どういうロジックで発生時期・発生好適日を計算しているか」をまとめた詳細ドキュメントです。

---

## 1. 全体の仕組み

1. `crops_InsectPests.json` の内容を埋め込んだ `CROP_DATA` から、作物とその主な病害虫の一覧を表示する。
2. 選ばれた病害虫名を、あらかじめ用意した `MODEL_LIBRARY`（発育零点・有効積算温度・発生好適条件などのデータベース）とキーワード照合し、該当するモデルを自動選択する。
3. 現在地（または手動入力した緯度経度）をもとに、Open-Meteoから気温・湿度・降水量・日射量の日別データを取得する。
4. モデルの種類（昆虫の積算温度モデル／病害の気温×湿度モデル／いもち病の簡易BLASTAMモデル）に応じて発生時期・発生好適日を計算し、グラフとテキストで表示する。

---

## 2. 使用している気象データ

| 変数 | 内容 | 用途 |
|---|---|---|
| `temperature_2m_mean` | 日平均気温(℃) | 積算温度計算、病害の気温条件判定 |
| `precipitation_sum` | 日降水量(mm) | 湿度データが欠測の場合のフォールバック判定 |
| `relative_humidity_2m_mean` | 日平均相対湿度(%) | 病害の多湿・乾燥条件判定 |
| `shortwave_radiation_sum` | 日積算日射量(MJ/m²) | 灰色かび病の「日照不足」判定（平年比） |

取得元は3種類のOpen-Meteo APIエンドポイントを使い分けています。

| 期間 | 取得方法 |
|---|---|
| 本日−92日 〜 本日+16日 | forecast API（`past_days=92&forecast_days=16`）の実測・予報値 |
| 本日−92日より前の過去日 | archive API（ERA5）の実測値 |
| 本日+16日より先の未来日、または実測が欠測の日 | 過去5年分（`(本年-5)-01-01`〜本日）を archive API から取得し、月日ごとに平均した「平年値」で補完 |

灰色かび病モデル（日照不足判定あり）を選択した場合は、上記の「平年値」を実測がある日についても常に取得し、その日の日射量が平年と比べてどの程度かを計算できるようにしています（通常の病害・昆虫モデルでは、この追加取得は行われません＝APIアクセスの節約）。

出典：[Open-Meteo](https://open-meteo.com/)（Forecast API / Historical Weather API、CC BY 4.0）

---

## 3. 作物・病害虫データと自動モデル判定

### 3.1 作物データ
`crops_InsectPests.json` に収録された26作物（トマト・キュウリ・ナス・ピーマン・キャベツ・レタス・ハクサイ・タマネギ・ニンジン・ダイコン・ホウレンソウ・ブロッコリー・ネギ・ジャガイモ・サツマイモ・カボチャ・スイカ・トウモロコシ・インゲン・エダマメ・オクラ・チンゲンサイ・コマツナ・イチゴ・ショウガ・イネ）について、それぞれの `main_pests`（主な害虫）・`main_diseases`（主な病害）の名称リストをそのままプルダウンに列挙しています。

### 3.2 モデルの自動判定（キーワード部分一致）
選択された病害虫の名称文字列に対して、`MATCH_RULES` に登録されたキーワードを先頭から順に部分一致で検索し、最初にヒットしたモデルを採用します。

```
["オオタバコガ", "tabakoga"]
["コナジラミ", "whitefly"]
["アザミウマ", "thrips"]
["ハスモンヨトウ", "armyworm"]
["ヨトウムシ", "armyworm"]
["コナガ", "diamondback"]
["ハダニ", "mite"]
["カメムシ", "stinkbug"]
["アブラムシ", "aphid"]
["いもち病", "blast"]
["うどんこ病", "powdery"]
["灰色かび病", "graymold"]
["べと病", "downy"]
["疫病", "blight"]
```

より特異的な語（例：「オオタバコガ」「ハスモンヨトウ」）を、広い語（「ヨトウムシ」等）より先に判定することで、誤って一般的なモデルに丸め込まれるのを防いでいます。

該当するキーワードが1つも無い病害虫（例：ウリハムシ、キスジノミハムシ、根こぶ病、モザイク病など）は「データ未登録」と表示され、カスタム入力（後述）に切り替わります。

---

## 4. 昆虫：積算温度（有効積算温度）モデル

### 4.1 計算式

```
有効温度(その日) = max(0, 日平均気温 − 発育零点)
積算有効温度      = 起算日からその日までの有効温度の合計
```

積算有効温度が「有効積算温度 × N」を超えた最初の日を、「第N世代 到達目安」としてグラフ・テキストに表示します（最大6世代分の閾値線を表示）。

### 4.2 登録済みモデル一覧

| モデル名 | 発育零点(℃) | 有効積算温度(日度) | 出典・備考 |
|---|---|---|---|
| アブラムシ類（モモアカアブラムシ等） | 5.0 | 135 | シンジェンタジャパン「害虫の活動と有効積算温度の関係」／カクイチ農業メディア「積算温度の活用法」。種・条件により発育零点約3〜10℃、有効積算温度約120〜150日度と幅がある代表値。 |
| アザミウマ類（チャノキイロアザミウマ） | 9.1 | 280 | シンジェンタジャパン「害虫の活動と有効積算温度の関係」。ネギアザミウマ等、他種では値が異なる。 |
| オオタバコガ | 8.4 | 450 | カクイチ農業メディア「積算温度の活用法」。卵の発育零点約8.4℃、卵〜成虫の有効積算温度は目安400〜500日度の中間値。 |
| カメムシ類（クモヘリカメムシ等） | 11.6 | 189 | 農林水産省 気候変動影響評価資料／カクイチ農業メディア。クモヘリカメムシの産卵〜羽化（河合, 1985）。種による差が大きい。 |
| コナジラミ類（オンシツコナジラミ） | 8.6 | 357 | セイコーエコロジア「オンシツコナジラミの対策と予防」（京都大学農学部昆虫学研究室の知見を引用）。範囲8.3〜8.9℃の中間値。 |
| ヨトウムシ類（ハスモンヨトウ） | 10.3 | 526 | 農林水産省 気候変動影響評価資料／植物防疫協会「ハスモンヨトウの海を越えた移動」。産卵〜羽化（Miyashita, 1971）。同種で628日度とする推定例もある。 |
| ハダニ類（ナミハダニ） | 10.0 | 180 | 農林水産省 気候変動影響評価資料。卵〜産卵開始（内田, 1982）。有効積算温度179.8日度を丸めた値。 |
| コナガ | 8.5 | 270 | 日本応用動物昆虫学会誌「コナガの発育零点と発育有効積算温量」／カクイチ農業メディア。個体群により発育零点7.4〜9.5℃・有効積算温度229〜313日度と地理的差があるため中間的な目安値。 |

### 4.3 カスタム入力
- 病害虫プルダウンで明示的に「🔧 カスタム（発育零点・有効積算温度を入力）」を選んだ場合。
- または、選んだ病害虫名がどのキーワードにも一致しなかった場合（モデル未登録）に、自動でカスタム入力欄が開きます。この場合、入力すればその値で積算計算が可能です（未入力のまま実行するとエラーメッセージを表示）。

---

## 5. 病害：気温×湿度モデル（うどんこ病・灰色かび病・べと病・疫病）

### 5.1 判定式

```
発生好適日 = (tempMin ≦ 日平均気温 ≦ tempMax) AND (湿度条件)
```

湿度条件は、モデルごとに「多湿型」（`rhMin`：この値以上で成立）と「乾燥型」（`rhMax`：この値以下で成立）のどちらかを設定しています。

湿度データ（`relative_humidity_2m_mean`）がその日だけ取得できなかった場合は、代わりに降水量で判定します（多湿型なら「降水量1mm以上」、乾燥型なら「降水量1mm未満」）。その場合は、結果表示欄に「湿度データが取得できなかった◯日は、降水量の有無で代用して判定しています」という注記が出ます。

### 5.2 登録済みモデル一覧

| モデル名 | 気温条件 | 湿度条件 | 追加条件 | 出典 |
|---|---|---|---|---|
| うどんこ病 | 17〜25℃ | 相対湿度70%以下（乾燥型） | なし | 茨城県病害虫防除所資料／各種病害虫辞典。うどんこ病菌は他の糸状菌病と異なり多湿でなくても発病しやすく、比較的低湿度でも発生する。 |
| 灰色かび病 | 15〜20℃ | 相対湿度85%以上（多湿型） | 日照不足（下記6章） | 茨城県「施設野菜共通-灰色カビ病」病害虫防除資料／セイコーエコロジア関連記事。 |
| べと病 | 15〜24℃ | 相対湿度85%以上（多湿型） | なし | 各種病害虫防除資料。 |
| 疫病 | 15〜24℃ | 相対湿度85%以上（多湿型） | なし | 各種病害虫防除資料。 |

---

## 6. 灰色かび病の「日照不足」判定ロジック

灰色かび病のみ、気温・湿度に加えて「日照不足が続いているか」を追加の条件にしています。

### 6.1 根拠
茨城県の病害虫防除資料に「本病は，20℃前後の温度で湿度が高く，**日照が少ないとき**に発生しやすくなる」と明記されているほか、複数の栽培技術解説記事でも同様の知見が確認できたため採用しています。

### 6.2 計算方法

```
平年比(その日) = その日の日射量 ÷ その日と同じ月日の過去5年平均日射量
直近3日平均平年比 = 判定日を含む直近3日間の「平年比」の単純平均
日照不足 = 直近3日平均平年比 < 0.7（70%未満）
```

- 「その日と同じ月日の過去5年平均」は、本年から5年前の1月1日〜本日までのarchive APIデータを月日（例："06-15"）ごとに集計して算出しています。
- 実測値または平年値のどちらかが欠けている日は、その日を平均の計算対象から除外します。直近3日間すべてのデータが欠けている場合は「判定不能」として、日照条件を満たしているものとみなします（気温・湿度の条件だけで判定）。
- 日射量についても、気温・降水量・湿度と同じ方針で「実測・予報があればそれを使い、無ければその日と同じ月日の過去5年平均で補完する」という扱いをしています。**本日+16日より先の未来日は日射量の実測・予報が存在しないため、常に過去5年平均で補完された値が使われます。**
- 補完された日は、定義上「平年値 ÷ 平年値 ＝ 100%」となるため、平年比は必ず100%（＝平年並み、日照不足ではない）と計算されます。これは不具合ではなく意図した挙動です。実測データが無い未来の日については「平年並みの日照であると仮定する」という考え方に基づいています。そのため、実測日が少なく補完日が続く期間（特に本日+16日より先）では、灰色かび病の「日照不足」条件はほぼ成立せず、気温・湿度の条件が実質的な判定基準になります。
- 平年値そのものが求まらない日（該当月日のarchive-apiデータが全く無い等、通常は起こりにくいケース）のみ、計算対象から除外されます。直近3日間すべてでこの状態になった場合は「判定不能」として、日照条件を満たしているものとみなします（気温・湿度の条件だけで判定）。

### 6.3 灰色かび病の最終判定式

```
発生好適日 = (15℃ ≦ 気温 ≦ 20℃) AND (相対湿度 ≧ 85%) AND (直近3日平均日照が平年比70%未満)
```

---

## 7. いもち病（葉いもち）：簡易BLASTAM判定

### 7.1 正式なBLASTAM方式
稲いもち病の感染好適条件判定として広く使われる「BLASTAM」方式は、次の3条件をすべて満たす日を好適日とします。

1. 葉の濡れ時間が8〜10時間以上
2. 濡れている間の気温が15〜25℃
3. 判定日を含む直近5日間の平均気温が20〜25℃

出典：高知県病害虫防除所／JA上伊那／京都府病害虫防除所のBLASTAM関連資料。

### 7.2 本ツールでの簡易化
Open-Meteoからは「葉の濡れ時間」を直接取得できないため、代わりに次の指標で近似しています。

```
濡れ日OK  = 日平均相対湿度 ≧ 90%（湿度データが無い日は、日降水量 ≧ 1mm で代用）
当日気温OK = 15℃ ≦ 日平均気温 ≦ 25℃
5日平均OK = 判定日を含む直近5日間の日平均気温の平均が20〜25℃

感染好適日（簡易判定） = 濡れ日OK AND 当日気温OK AND 5日平均OK
```

降水量ではなく相対湿度を「濡れ日」の代用に採用しているのは、降水量よりも相対湿度の方が葉面が湿った状態にどれだけ近いかをより直接的に反映すると考えられるためです。ただし、実際の葉面湿潤時間の実測値とは異なる簡易指標である点に注意してください。

---

## 8. カスタム入力について

- 昆虫（積算温度モデル）についてのみカスタム入力に対応しています。発育零点・有効積算温度の2つの数値を入力すると、同じ積算温度ロジックで計算します。
- 病害（気温×湿度モデル）・いもち病については、カスタム入力には対応していません（気象条件モデルは病害虫ごとに構造が異なり、単純な数値2つでは表現しきれないため）。

---

## 9. 出力機能

### 9.1 グラフ
- 昆虫モデル：積算有効温度の折れ線グラフ＋世代閾値の破線（最大6世代分）。
- 病害・いもち病モデル：発生好適日／感染好適日を0/1の棒グラフで表示し、日平均気温の折れ線、取得できていれば日平均相対湿度の点線を重ねて表示します。灰色かび病を選択した場合はさらに、「日射量（実測・予報／欠測日は平年補完） (MJ/m²)」の折れ線と、比較用の「日射量 平年値 (MJ/m²)」の破線を重ねて表示します（日照不足の判定自体は、これらの値から計算する平年比70%未満かどうかで行われますが、グラフには判定に使う比率(%)ではなく、実際の日射量・平年値をそのまま表示します）。

### 9.2 CSVダウンロード
- 昆虫モデル：日付・日平均気温・有効温度・積算有効温度・実測/予報または過去5年平均補完の別。
- 病害・いもち病モデル：日付・日平均気温・日平均相対湿度・日降水量・（灰色かび病のみ）日射量と平年比(%)・該当有無・実測/予報または過去5年平均補完の別。

### 9.3 PNGダウンロード
グラフを透かし入りのPNG画像として保存できます。透かしには、取得できていれば地名（市区町村名＋大字名）、できていなければ緯度・経度を表示します。

---

## 10. 位置情報の取得方法

1. ブラウザの位置情報（Geolocation API）で緯度・経度を取得。
2. 国土地理院(GSI)の逆ジオコーダAPI（`https://mreversegeocoder.gsi.go.jp/reverse-geocoder/LonLatToAddress`）と、市区町村コード変換テーブル（`https://maps.gsi.go.jp/js/muni.js`）を使って、市区町村名・大字名まで解決。
3. 位置情報が使えない場合や、他の地点を指定したい場合は、緯度・経度を手動入力できます。
4. 地名が解決できない場所（離島・一部山間部等）では、座標のみを表示します。

---

## 11. 既知の制限・注意事項

- 表示される発育零点・有効積算温度・発生好適条件は、公表資料に基づく**代表的な参考値**です。地域・品種・年次・害虫の系統によって実際の値は異なります。
- `crops_InsectPests.json` に収録されている病害虫名（合計238件）のうち、キーワードでモデルに自動照合できるのは約半数（約123件）です。残りは「データ未登録」と表示され、カスタム入力（昆虫のみ）でご利用いただけます。
- 灰色かび病以外の病害（うどんこ病・べと病・疫病）には日照条件を組み込んでいません。同様に文献的根拠が確認できた病害があれば、追加で組み込むことができます。
- いもち病の「濡れ日」判定は、正式なBLASTAM方式（葉面湿潤時間の実測）とは異なる簡易的な代用指標です。
- **実際の防除の判断は、必ずお住まいの都道府県病害虫防除所が発表する「病害虫発生予察情報」もあわせてご確認ください。**

---

## 12. 出典一覧（まとめ）

- Open-Meteo（Forecast API / Historical Weather API）：https://open-meteo.com/ （CC BY 4.0）
- 国土地理院（GSI）：逆ジオコーダAPI・市区町村変換テーブル（muni.js）
- シンジェンタジャパン「害虫の活動と有効積算温度の関係」
- カクイチ農業メディア「積算温度の活用法」
- セイコーエコロジア「オンシツコナジラミの対策と予防」
- セイコーエコロジア「灰色カビ病の原因とは？発生条件と感染の仕組みを解説」ほか関連記事
- 日本応用動物昆虫学会誌「コナガの発育零点と発育有効積算温量」
- 農林水産省 気候変動影響評価資料
- 植物防疫協会「ハスモンヨトウの海を越えた移動」
- 茨城県病害虫防除所資料（うどんこ病、灰色カビ病ほか）
- 茨城県「施設野菜共通-灰色カビ病」病害虫防除資料
- 高知県病害虫防除所／JA上伊那／京都府病害虫防除所（BLASTAM関連資料）
- 各種病害虫防除資料・病害虫辞典
